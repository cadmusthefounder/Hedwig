<li class="review">
  <%= link_to gravatar_for(review.owner, size: 50), review.owner %>
  <header>
    <span class="owner h3">
      <%= link_to review.owner.name, review.owner %>
    </span>
    <span class="star-rating" data-score="<%= review.rating %>" data-read-only="true"></span>
  </header>
  <p class="comment"><%= review.comment %></p>
  <p class="timestamp">
    Posted <%= time_ago_in_words(review.updated_at) %> ago.
  </p>
</li>
